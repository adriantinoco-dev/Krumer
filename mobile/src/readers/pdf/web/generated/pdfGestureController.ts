export const PDF_WEB_GESTURE_CONTROLLER_SOURCE = "function createPdfGestureController(options) {\n    var viewer = options.viewer;\n    var post = options.post;\n    var clampScale = options.clampScale;\n    var getScale = options.getScale;\n    var commitScale = options.commitScale;\n    var getCurrentPage = options.getCurrentPage;\n    var now = options.now || function () { return Date.now(); };\n    var requestFrame = options.requestFrame || function (callback) { return requestAnimationFrame(callback); };\n    var cancelFrame = options.cancelFrame || function (frame) { cancelAnimationFrame(frame); };\n    var pointers = new Map();\n    var surfaces = new Set();\n    var nextSurfaceId = 0;\n    var mode = 'idle';\n    var primaryId = null;\n    var pinch = null;\n    var panFrame = null;\n    var panDx = 0;\n    var panDy = 0;\n    var pinchFrame = null;\n    var pendingPinch = null;\n    var inertiaFrame = null;\n    var suppressTapUntil = 0;\n    var suppressClickUntil = 0;\n    var PAN_START_PX = 8;\n    var TAP_MAX_MOVEMENT_PX = 14;\n    var TAP_MAX_DURATION_MS = 280;\n    var SWIPE_MIN_VELOCITY = 0.45;\n    function prevent(event) {\n        if (event && event.cancelable !== false)\n            event.preventDefault?.();\n    }\n    function viewerRect() {\n        return viewer?.getBoundingClientRect?.() || { left: 0, top: 0 };\n    }\n    function pointFor(event, surface) {\n        var root = viewerRect();\n        var x = Number(event?.clientX) || 0;\n        var y = Number(event?.clientY) || 0;\n        if (surface.frame?.getBoundingClientRect) {\n            var frameRect = surface.frame.getBoundingClientRect();\n            // Pointer coordinates inside an iframe are untransformed CSS pixels.\n            // Include its live pinch scale when mapping them back to the viewport.\n            var scaleX = surface.frame.clientWidth ? frameRect.width / surface.frame.clientWidth : 1;\n            var scaleY = surface.frame.clientHeight ? frameRect.height / surface.frame.clientHeight : 1;\n            x = frameRect.left + x * scaleX;\n            y = frameRect.top + y * scaleY;\n        }\n        return { x: x - root.left, y: y - root.top };\n    }\n    function selectionExists(surface) {\n        try {\n            var selection = surface.doc?.getSelection?.();\n            return Boolean(selection && String(selection).length);\n        }\n        catch (_) {\n            return false;\n        }\n    }\n    function anchorFor(event) {\n        return event?.target?.closest?.('a') || null;\n    }\n    function capture(pointer) {\n        try {\n            pointer.captureTarget?.setPointerCapture?.(pointer.pointerId);\n        }\n        catch (_) { /* no-op */ }\n    }\n    function release(pointer) {\n        try {\n            pointer.captureTarget?.releasePointerCapture?.(pointer.pointerId);\n        }\n        catch (_) { /* no-op */ }\n    }\n    function cancelInertia() {\n        if (inertiaFrame != null)\n            cancelFrame(inertiaFrame);\n        inertiaFrame = null;\n    }\n    function flushPan() {\n        panFrame = null;\n        if (!panDx && !panDy)\n            return;\n        var dx = panDx;\n        var dy = panDy;\n        panDx = 0;\n        panDy = 0;\n        viewer?.pan?.(dx, dy);\n    }\n    function queuePan(dx, dy) {\n        panDx += dx;\n        panDy += dy;\n        if (panFrame == null)\n            panFrame = requestFrame(flushPan);\n    }\n    function flushPinch() {\n        pinchFrame = null;\n        if (!pendingPinch)\n            return;\n        var preview = pendingPinch;\n        pendingPinch = null;\n        viewer?.pinchZoom?.(preview.ratio, preview.focal);\n    }\n    function queuePinch(preview) {\n        pendingPinch = preview;\n        if (pinchFrame == null)\n            pinchFrame = requestFrame(flushPinch);\n    }\n    function firstTwoPointers() {\n        return Array.from(pointers.values()).slice(0, 2);\n    }\n    function midpoint(first, second) {\n        return { x: (first.x + second.x) / 2, y: (first.y + second.y) / 2 };\n    }\n    function distance(first, second) {\n        return Math.hypot(second.x - first.x, second.y - first.y);\n    }\n    function clearSelections() {\n        surfaces.forEach(function (surface) {\n            try {\n                surface.doc?.getSelection?.()?.removeAllRanges?.();\n            }\n            catch (_) { /* no-op */ }\n        });\n    }\n    function beginPinch(event) {\n        var pair = firstTwoPointers();\n        if (pair.length < 2 || !viewer?.pinchZoom)\n            return false;\n        var initialDistance = distance(pair[0], pair[1]);\n        if (!(initialDistance > 0))\n            return false;\n        cancelInertia();\n        if (panFrame != null) {\n            cancelFrame(panFrame);\n            panFrame = null;\n            panDx = 0;\n            panDy = 0;\n        }\n        var focal = midpoint(pair[0], pair[1]);\n        mode = 'pinch';\n        primaryId = null;\n        pinch = {\n            initialDistance: initialDistance,\n            initialFocal: focal,\n            currentFocal: focal,\n            initialScale: getScale(),\n            ratio: 1,\n            startedAt: now(),\n        };\n        pointers.forEach(capture);\n        clearSelections();\n        suppressTapUntil = now() + 450;\n        suppressClickUntil = now() + 450;\n        viewer.pinchZoom(1, { x: focal.x, y: focal.y, deltaX: 0, deltaY: 0 });\n        prevent(event);\n        return true;\n    }\n    function updatePinch() {\n        if (mode !== 'pinch' || !pinch)\n            return;\n        var pair = firstTwoPointers();\n        if (pair.length < 2)\n            return;\n        var nextDistance = distance(pair[0], pair[1]);\n        if (!(nextDistance > 0))\n            return;\n        var focal = midpoint(pair[0], pair[1]);\n        var nextScale = clampScale(pinch.initialScale * nextDistance / pinch.initialDistance);\n        pinch.ratio = nextScale / pinch.initialScale;\n        pinch.currentFocal = focal;\n        queuePinch({\n            ratio: pinch.ratio,\n            focal: {\n                x: pinch.initialFocal.x,\n                y: pinch.initialFocal.y,\n                deltaX: focal.x - pinch.initialFocal.x,\n                deltaY: focal.y - pinch.initialFocal.y,\n            },\n        });\n    }\n    function finishPinch() {\n        if (!pinch)\n            return;\n        if (pinchFrame != null) {\n            cancelFrame(pinchFrame);\n            pinchFrame = null;\n        }\n        flushPinch();\n        var state = pinch;\n        var focal = state.currentFocal || state.initialFocal;\n        var nextScale = clampScale(state.initialScale * (state.ratio || 1));\n        viewer?.pinchEnd?.({ x: focal.x, y: focal.y });\n        commitScale(nextScale, Math.max(0, now() - state.startedAt));\n        pinch = null;\n        pendingPinch = null;\n        mode = pointers.size ? 'post-pinch' : 'idle';\n        suppressTapUntil = now() + 450;\n        suppressClickUntil = now() + 450;\n    }\n    function startInertia(vx, vy) {\n        if (!viewer?.scrolled || Math.hypot(vx, vy) < 0.08)\n            return;\n        var lastAt = null;\n        function step(timestamp) {\n            if (pointers.size || mode !== 'idle') {\n                inertiaFrame = null;\n                return;\n            }\n            if (lastAt == null)\n                lastAt = timestamp;\n            var elapsed = Math.max(1, Math.min(32, timestamp - lastAt));\n            lastAt = timestamp;\n            viewer.pan?.(vx * elapsed, vy * elapsed);\n            var decay = Math.pow(0.92, elapsed / 16.67);\n            vx *= decay;\n            vy *= decay;\n            if (Math.hypot(vx, vy) < 0.02) {\n                inertiaFrame = null;\n                return;\n            }\n            inertiaFrame = requestFrame(step);\n        }\n        inertiaFrame = requestFrame(step);\n    }\n    function finishPan(pointer) {\n        if (panFrame != null) {\n            cancelFrame(panFrame);\n            panFrame = null;\n        }\n        flushPan();\n        var dx = pointer.x - pointer.startX;\n        var dy = pointer.y - pointer.startY;\n        var absX = Math.abs(dx);\n        var absY = Math.abs(dy);\n        var width = Math.max(1, Number(viewer?.clientWidth) || 360);\n        var swipeDistance = Math.max(36, Math.min(96, width * 0.12));\n        var fastDistance = Math.max(24, width * 0.06);\n        var horizontal = absX > absY * 1.25;\n        var fastEnough = Math.abs(pointer.fingerVx) >= SWIPE_MIN_VELOCITY && absX >= fastDistance;\n        var canTurnPage = !viewer?.scrolled && getScale() <= 1.001;\n        if (canTurnPage && horizontal && (absX >= swipeDistance || fastEnough)) {\n            var forward = viewer?.rtl ? dx > 0 : dx < 0;\n            if (forward)\n                viewer?.next?.();\n            else\n                viewer?.prev?.();\n        }\n        else if (viewer?.scrolled) {\n            startInertia(pointer.scrollVx, pointer.scrollVy);\n        }\n        suppressTapUntil = now() + 350;\n        suppressClickUntil = now() + 350;\n    }\n    function resetPointerState() {\n        pointers.forEach(release);\n        pointers.clear();\n        primaryId = null;\n        pinch = null;\n        mode = 'idle';\n        panDx = 0;\n        panDy = 0;\n        pendingPinch = null;\n        if (panFrame != null)\n            cancelFrame(panFrame);\n        if (pinchFrame != null)\n            cancelFrame(pinchFrame);\n        panFrame = null;\n        pinchFrame = null;\n    }\n    function onPointerDown(event, surface) {\n        if (event.button !== undefined && event.button !== 0)\n            return;\n        cancelInertia();\n        var id = event.pointerId;\n        if (id === undefined || pointers.has(id))\n            return;\n        var point = pointFor(event, surface);\n        var timestamp = now();\n        var pointer = {\n            pointerId: id,\n            captureTarget: event.target,\n            surface: surface,\n            startX: point.x,\n            startY: point.y,\n            x: point.x,\n            y: point.y,\n            lastX: point.x,\n            lastY: point.y,\n            startedAt: timestamp,\n            lastAt: timestamp,\n            fingerVx: 0,\n            fingerVy: 0,\n            scrollVx: 0,\n            scrollVy: 0,\n            link: anchorFor(event),\n        };\n        pointers.set(id, pointer);\n        if (pointers.size === 1) {\n            mode = 'pending';\n            primaryId = id;\n        }\n        else if (pointers.size >= 2) {\n            beginPinch(event);\n        }\n    }\n    function updatePointer(event, surface) {\n        var pointer = pointers.get(event.pointerId);\n        if (!pointer)\n            return null;\n        var point = pointFor(event, surface);\n        var timestamp = now();\n        var elapsed = Math.max(1, timestamp - pointer.lastAt);\n        pointer.lastX = pointer.x;\n        pointer.lastY = pointer.y;\n        pointer.x = point.x;\n        pointer.y = point.y;\n        pointer.lastAt = timestamp;\n        var movedX = pointer.x - pointer.lastX;\n        var movedY = pointer.y - pointer.lastY;\n        if (movedX || movedY) {\n            pointer.fingerVx = movedX / elapsed;\n            pointer.fingerVy = movedY / elapsed;\n            pointer.scrollVx = -pointer.fingerVx;\n            pointer.scrollVy = -pointer.fingerVy;\n        }\n        return pointer;\n    }\n    function onPointerMove(event, surface) {\n        var pointer = updatePointer(event, surface);\n        if (!pointer)\n            return;\n        if (mode === 'pinch') {\n            updatePinch();\n            prevent(event);\n            return;\n        }\n        if (pointers.size >= 2 && beginPinch(event))\n            return;\n        if (mode === 'post-pinch' || mode === 'selection') {\n            if (mode === 'post-pinch')\n                prevent(event);\n            return;\n        }\n        if (selectionExists(pointer.surface)) {\n            mode = 'selection';\n            return;\n        }\n        if (pointer.pointerId !== primaryId)\n            return;\n        var totalDistance = Math.hypot(pointer.x - pointer.startX, pointer.y - pointer.startY);\n        if (mode === 'pending' && totalDistance >= PAN_START_PX) {\n            mode = 'pan';\n            capture(pointer);\n        }\n        if (mode === 'pan') {\n            queuePan(pointer.lastX - pointer.x, pointer.lastY - pointer.y);\n            prevent(event);\n        }\n    }\n    function finishPointer(event, surface, cancelled) {\n        var pointer = updatePointer(event, surface) || pointers.get(event.pointerId);\n        if (!pointer)\n            return;\n        var activeMode = mode;\n        pointers.delete(event.pointerId);\n        release(pointer);\n        if (activeMode === 'pinch') {\n            prevent(event);\n            if (pointers.size < 2)\n                finishPinch();\n            if (!pointers.size)\n                mode = 'idle';\n            return;\n        }\n        if (activeMode === 'post-pinch') {\n            prevent(event);\n            if (!pointers.size)\n                mode = 'idle';\n            return;\n        }\n        if (cancelled) {\n            if (!pointers.size)\n                resetPointerState();\n            prevent(event);\n            return;\n        }\n        if (activeMode === 'selection' || selectionExists(pointer.surface)) {\n            if (!pointers.size)\n                mode = 'idle';\n            return;\n        }\n        if (activeMode === 'pan' && pointer.pointerId === primaryId) {\n            finishPan(pointer);\n            primaryId = null;\n            mode = pointers.size ? 'post-pan' : 'idle';\n            prevent(event);\n            return;\n        }\n        if (activeMode === 'post-pan') {\n            prevent(event);\n            if (!pointers.size)\n                mode = 'idle';\n            return;\n        }\n        primaryId = null;\n        mode = pointers.size ? mode : 'idle';\n        var duration = now() - pointer.startedAt;\n        var movement = Math.hypot(pointer.x - pointer.startX, pointer.y - pointer.startY);\n        if (pointer.link || now() < suppressTapUntil || duration > TAP_MAX_DURATION_MS || movement > TAP_MAX_MOVEMENT_PX)\n            return;\n        var width = Math.max(1, Number(viewer?.clientWidth) || 1);\n        var height = Math.max(1, Number(viewer?.clientHeight) || 1);\n        var page = Number.isInteger(pointer.surface.index) ? pointer.surface.index + 1 : getCurrentPage();\n        post('SINGLE_TAP', {\n            page: page,\n            x: Math.max(0, Math.min(width, pointer.x)),\n            y: Math.max(0, Math.min(height, pointer.y)),\n        });\n    }\n    function attach(target, config) {\n        if (!target || target.__krumerPdfGestureSurface)\n            return target?.__krumerPdfGestureSurface?.detach;\n        var surface = {\n            id: ++nextSurfaceId,\n            target: target,\n            doc: config?.doc || (target.nodeType === 9 ? target : null),\n            frame: config?.frame || (target.nodeType === 9 ? target.defaultView?.frameElement : null),\n            index: config?.index,\n            root: Boolean(config?.root),\n        };\n        var down = function (event) { onPointerDown(event, surface); };\n        var move = function (event) { onPointerMove(event, surface); };\n        var up = function (event) { finishPointer(event, surface, false); };\n        var cancel = function (event) { finishPointer(event, surface, true); };\n        var click = function (event) {\n            if (now() < suppressClickUntil) {\n                event.preventDefault?.();\n                event.stopImmediatePropagation?.();\n            }\n        };\n        target.addEventListener('pointerdown', down, { passive: false });\n        target.addEventListener('pointermove', move, { passive: false });\n        target.addEventListener('pointerup', up, { passive: false });\n        target.addEventListener('pointercancel', cancel, { passive: false });\n        target.addEventListener('lostpointercapture', cancel, { passive: false });\n        target.addEventListener('click', click, true);\n        surface.detach = function () {\n            target.removeEventListener('pointerdown', down);\n            target.removeEventListener('pointermove', move);\n            target.removeEventListener('pointerup', up);\n            target.removeEventListener('pointercancel', cancel);\n            target.removeEventListener('lostpointercapture', cancel);\n            target.removeEventListener('click', click, true);\n            surfaces.delete(surface);\n            try {\n                delete target.__krumerPdfGestureSurface;\n            }\n            catch (_) { /* no-op */ }\n        };\n        target.__krumerPdfGestureSurface = surface;\n        surfaces.add(surface);\n        return surface.detach;\n    }\n    function resetFrames() {\n        cancelInertia();\n        resetPointerState();\n        Array.from(surfaces).forEach(function (surface) {\n            if (!surface.root)\n                surface.detach();\n        });\n    }\n    function destroy() {\n        resetFrames();\n        Array.from(surfaces).forEach(function (surface) { surface.detach(); });\n    }\n    return { attach: attach, destroy: destroy, resetFrames: resetFrames };\n}";
