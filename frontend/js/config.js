// Cloud authentication and synchronization are frozen for the beta release.
// Keep this as a fail-closed flag so the existing implementation can be
// re-enabled deliberately after the beta.
window.KRUMER_CLOUD_SYNC_ENABLED = false;
